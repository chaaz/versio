

```
commands:
  check
  get --prev --name comb --wide
  get --prev --id 4
  show --prev --wide
  set --name comb --value 1.2.3
  set --id comb --value 1.2.3
  diff --no-fetch
  files --no-fetch
  plan --no-fetch
  run --no-fetch --dry-run
```
